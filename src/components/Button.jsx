function Button({ children, color, isPressed, onClick }) {
  // TODO (Iteracion 1.3): devuelve un <button> con:
  //   - className compuesto: `btn btn-${color}` y, ademas, " active"
  //     unicamente cuando isPressed sea true.
  //   - onClick = onClick.
  //   - children como contenido del boton.
  //
  // Pista: una template literal funciona bien aqui, por ejemplo:
  //   className={`btn btn-${color} ${isPressed ? "active" : ""}`}
  return null;
}

export default Button;
